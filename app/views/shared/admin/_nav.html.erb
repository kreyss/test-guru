<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/admin">Test Guru</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/admin/tests"><%= t('buttons.all_tests') %></a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="/admin/tests/new"><%= t('buttons.new_test') %></a>
      </li>
    </ul>
  </div>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <b><%= current_user.first_name %></b>
      <%= link_to t('buttons.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('buttons.login'), user_session_path %>
      <%= link_to t('buttons.signup'), new_user_registration_path %>
    <% end %>
  </span>
</nav> 
