<nav class="navbar navbar-light bg-light">
  <%= link_to 'Test Guru', root_path, class: 'navbar-brand' %>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= t('welcome') %><b><%= current_user.first_name %></b>!
      <%= link_to t('buttons.logout'), destroy_user_session_path, method: :delete, class: '' %>
    <% else %>
      <%= link_to t('buttons.login'), user_session_path %>
      <%= link_to t('buttons.signup'), new_user_registration_path %>
    <% end %>
  </span>
</nav> 
